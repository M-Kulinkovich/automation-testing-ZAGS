name: Java CI Autotesting

on:
  push:
    branches:
      - "Task_6_Cucumber/Gherkin_CI"
  pull_request:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up secret variables
        run: |
          echo "DB_URL=${{ secrets.BASE_URL }}" >> $GITHUB_ENV
          echo "DB_USER=${{ secrets.DBHOST }}" >> $GITHUB_ENV
          echo "DB_PASSWORD=${{ secrets.DBNAME }}" >> $GITHUB_ENV
          echo "DB_PASSWORD=${{ secrets.DBPASS }}" >> $GITHUB_ENV
          echo "DB_PASSWORD=${{ secrets.DBPORT }}" >> $GITHUB_ENV
          echo "DB_PASSWORD=${{ secrets.DBSCHEMA }}" >> $GITHUB_ENV
          echo "DB_PASSWORD=${{ secrets.DBUSER }}" >> $GITHUB_ENV

      - name: Build with Maven
        run: mvn clean test
